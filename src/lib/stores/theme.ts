// Import the basic mode store from mode-watcher
import { mode } from 'mode-watcher';

export { mode };

// For now, just export the basic mode store
// Other stores can be added as needed from mode-watcher/stores if required
